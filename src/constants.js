// 서버, 클라이언트, 내가 가지고 있는 버전이 같아야 하는데 어디서 체크해야할까?
// 이곳은 버전 관리를 위한 상수가 저장되는 곳

// 배열 안에 있는 버전이 맞는 파일만 실행이 되어야 한다
// 현재 접속 가능한 버전
export const CLIENT_VERSION = ['1.0.0', '1.0.1', '1.1.0'];

export default CLIENT_VERSION;
